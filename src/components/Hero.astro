---
import SocialPill from "./SocialPill.astro";
import { Icon } from "astro-icon/components";

const data = {
  position: "Desarrollador Full Stack",
  name: "Stephano Fernandez 👋🏻",
  social: {
    linkedin: "https://www.linkedin.com/in/stephanofer/",
    github: "https://github.com/stephanofer",
    instagram: "https://www.instagram.com/stephanofer/",
    phone: "tel:+51981314610",
    mail: "mailto:contacto@stephanofer.com"
  }
};
---

<div class="container">
  <div class="hero">
    <div class="openwork">Disponible para trabajar</div>
    <h1>{data.position}</h1>
    <h2>{data.name}</h2>
    <p>
      <strong>+1 año</strong> de experiencia en la creación de soluciones para empresas, abarcando el ciclo completo de desarrollo desde la conceptualización hasta la <strong>implementación exitosa en producción</strong>.
    </p>

    <div class="container-item contact">
      <div class="icons">
        <div>
          <SocialPill href={data.social.linkedin}>
            <span class="sr-only">LinkedIn</span>
            <Icon name="linkedin" size="20" />
          </SocialPill>

          <SocialPill href={data.social.github}>
            <span class="sr-only">Github</span>
            <Icon name="github" size="20" />
          </SocialPill>

          <SocialPill href={data.social.instagram}>
            <span class="sr-only">Instagram</span>
            <Icon name="instagram" size="20" />
          </SocialPill>
        </div>

        <div>
          <SocialPill href={data.social.phone}>
            <span class="sr-only">Telefono</span>
            <Icon name="phone" size="20" />
            <span>+51 981 314 610</span>
          </SocialPill>
          <SocialPill href={data.social.mail}>
            <span class="sr-only">Correo</span>
            <Icon name="email" size="20" />
            <span>contacto@stephanofer.com</span>
          </SocialPill>
        </div>
      </div>
    </div>
  </div>

  <div class="container-social">
    <div class="image">
      <img src="/assets/me-profile.png" alt="Stephano Fernandez Foto" />
    </div>
    <a class="download" href="/resume.pdf" target = '_blank' role = "link">
      <Icon name="download" size="20" />
      Descargar CV
    </a>
  </div>
</div>


<style>
  .openwork {
    background-color: rgb(32 73 30 / 0.5);
    display: inline-flex;
    align-items: center;
    justify-self: center;
    gap: 0.3rem;
    padding-left: 1rem;
    padding-right: 1rem;
    padding-top: 0.4rem;
    padding-bottom: 0.4rem;
    border-radius: 99999px;
    font-size: var(--fs-sm);
    cursor: pointer;
    transition:
      transform 200ms,
      background-color 200ms;
    color: var(--pastel-green-200);
    white-space: nowrap;
    margin-bottom: 1rem;
    &::before {
      --size: 10px;
      content: " ";
      display: inline-block;
      border-radius: 50%;

      width: var(--size);
      height: var(--size);
      background-color: var(--pastel-green-200);
      box-shadow: 0 0 5px var(--pastel-green-400);
    }

    &:hover {
      transform: scale(1.03);
      background-color: rgb(32 73 30 / 0.7);
    }
  }

  .container {
    display: flex;
    flex-direction: column;
    align-items: center;
    width: 100%;
    gap: 2rem;
    
    margin-bottom: 5rem;
    margin-top: 5rem;


    @media (width > 640px) {
      justify-content: space-between;
      flex-direction: row;
    }
  }

  .hero {
    max-width: 70ch;
  }

  h1 {
    font-size: var(--fs-lg);
    font-weight: 700;
    @media (width > 640px) {
      font-size: var(--fs-xxl);
    }
  }

  h2 {
    font-size: var(--fs-lg);
    font-weight: 400;

    margin-top: 0.5rem;

    @media (width > 640px) {
      font-size: var(--fs-xl);
    }
  }

  p {
    font-size: 20px;
    margin-top: 1rem;
    text-wrap-style: pretty;
    line-height: 1.5rem;
    color: var(--foreground);
    @media (width > 640px) {
      font-size: var(--fs-md);
    }
  }

  .container-social {
    display: flex;
    flex-direction: column;
  }

  .container-item {
    display: flex;
    flex-direction: column;
    margin-top: 2rem;
    border-radius: 0.7rem;
  }

  .icons {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    width: 100%;

    & > div:nth-child(1) {
      display: flex;
      flex-direction: row;
      align-items: center;
      justify-content: center;
      gap: 0.7rem;
      

      @media (width > 640px) {
      justify-content: start;
    }
    }

    & > div:nth-child(2) {
      display: flex;
      flex-direction: column;
      /* justify-content: center; */
      /* align-items:center; */
      gap: 0.5rem;


      @media (width > 640px) {
      flex-direction: row;
    }
    }
  }

  span {
    font-size: var(--fs-sm);
    text-wrap: nowrap;
  }

  .download {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    font-size: var(--fs-sm);
    padding-top: 0.6rem;
    padding-bottom: 0.6rem;
    border-radius: 0.3rem;
    margin-top: 1rem;

    background-color: rgb(32 73 30 / 0.5);
    color: var(--pastel-green-200);

    &:hover {
      background-color: rgb(32 73 30 / 0.7);
    }
  }

  img {
    --size: 350px;

    width: var(--size);
    height: var(--size);

    object-fit: cover;
  }

  strong{
    color: var(--resalt);
    font-weight: 600;
  }
</style>
